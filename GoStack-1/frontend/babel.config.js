module.exports = {
    presets: [
        '@babel/preset-env',
        '@babel/preset-react' //permite escrever HTML dentro do JavaScript
    ],
    plugins: [
        '@babel/plugin-transform-runtime'
    ]
};